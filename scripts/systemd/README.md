* coffee.service is a symbolic link to /etc/systemd/system/coffee.service
* check the status of the service via : `systemctl status coffee.service`
* Turn in on/off via `systemctl start/stop coffee.service`
